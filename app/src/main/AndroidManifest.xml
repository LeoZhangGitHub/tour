<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.tour">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <activity android:name=".LoginActivity"
            android:label="登录">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

       <!-- <activity
            android:name=".LoginActivity"
            android:label="登录" >

        </activity>-->

        <activity android:name=".ShareSomething"
            android:label="发表">

        </activity>

        <activity
            android:name="com.example.administrator.tour.MainActivity"
            android:label="@string/app_name"></activity>

        <activity android:name=".ArticleActivity"></activity>
        <activity android:name=".homepage.HomepageActivity"></activity>
        <activity android:name=".homepage.reserveAcitivity.SiteReserveActivity"></activity>
        <activity android:name=".homepage.reserveAcitivity.HotelActivity"></activity>
    </application>

</manifest>